# 在线订餐系统实训报告

## 一、基本情况

### 1.1 实训目的
通过实际项目开发，掌握JavaEE企业级应用开发技术，熟悉前后端分离的开发模式，理解在线订餐系统的业务流程和实现原理，提升团队协作和项目管理能力。

### 1.2 实训时间
2025年12月15日 - 2026年1月5日（共3周）

### 1.3 实训地点
学校计算机实验中心

### 1.4 合作企业
无

### 1.5 分组情况
本次实训为个人独立完成项目开发。

## 二、实训内容

### 2.1 准备工作
在实训开始前，我做了以下准备工作：
- 学习Spring Boot框架的基本原理和使用方法
- 掌握MyBatis ORM框架的配置和使用
- 学习Vue.js前端框架的核心概念和组件化开发
- 了解前后端分离开发模式的特点和优势
- 熟悉MySQL数据库的设计和优化
- 安装并配置开发环境：JDK 11、Maven、Node.js、Vue CLI、IntelliJ IDEA、VS Code

### 2.2 实训过程
实训过程分为以下几个阶段：

#### 阶段一：需求分析与数据库设计（第1周）
- 分析在线订餐系统的核心业务需求
- 设计系统的功能模块和数据流
- 设计数据库表结构，包括用户表、商家表、食品表、购物车表、订单表等
- 编写数据库初始化脚本

#### 阶段二：后端服务开发（第1-2周）
- 创建Spring Boot项目，配置项目依赖
- 实现实体类（Entity）
- 开发数据访问层（Mapper）
- 实现业务逻辑层（Service）
- 开发RESTful API接口（Controller）
- 实现文件上传功能
- 配置跨域支持

#### 阶段三：前端应用开发（第2-3周）
- 创建Vue.js项目，配置项目依赖
- 设计前端页面布局和路由
- 实现用户登录、注册功能
- 开发商家列表和详情页面
- 实现食品展示和购物车功能
- 开发订单管理和支付功能
- 实现用户地址管理

#### 阶段四：系统测试与优化（第3周）
- 进行单元测试和集成测试
- 修复系统bug和优化性能
- 完善系统文档

### 2.3 技术要点

#### 2.3.1 后端技术栈
- **Spring Boot 2.7.x**：用于构建Java企业级应用的框架，提供自动配置和快速开发能力
- **MyBatis 3.x**：ORM框架，用于简化数据库操作，通过XML配置实现SQL映射
- **MySQL 8.x**：关系型数据库，用于存储系统数据，支持事务和复杂查询
- **Maven**：项目构建和依赖管理工具，简化项目构建流程
- **RESTful API**：采用RESTful设计风格，提供统一规范的接口

#### 2.3.2 前端技术栈
- **Vue.js 3.x**：前端JavaScript框架，用于构建用户界面，采用组件化开发模式
- **Element Plus**：基于Vue 3的UI组件库，提供丰富的界面组件
- **Vue Router**：Vue.js的官方路由管理器，实现页面跳转和路由配置
- **Axios**：用于发送HTTP请求的库，实现前后端数据交互
- **Node.js**：JavaScript运行环境，支持前端项目的构建和运行
- **npm/yarn**：前端包管理工具，管理项目依赖

#### 2.3.3 核心功能实现

**用户管理**
- 用户注册、登录功能（使用表单验证和密码加密）
- 用户信息管理（个人资料修改、头像上传）
- 地址管理（增删改查用户配送地址）

**商家管理**
- 商家列表展示（支持分页和分类筛选）
- 商家详情查看（展示商家信息、食品分类和食品列表）

**食品管理**
- 食品分类展示（按分类组织食品）
- 食品详情查看（展示食品图片、价格、描述等信息）

**购物车**
- 添加/删除商品（支持多数量添加）
- 修改商品数量（增加/减少购物车中商品数量）
- 购物车结算（计算总价、生成订单）

**订单管理**
- 创建订单（从购物车生成订单）
- 订单列表查看（查看历史订单）
- 订单详情查看（查看订单中的食品和配送信息）

**文件上传**
- 商家图片上传（支持商家Logo和封面图片）
- 食品图片上传（支持食品展示图片）
- 用户头像上传（支持用户个人头像）

## 三、实训成果

### 3.1 系统架构

#### 后端架构
```
src/main/java/org/example/elm/
├── config/          # 配置类
├── controller/      # 控制器层
├── entity/          # 实体类
├── mapper/          # 数据访问层
├── service/         # 业务逻辑层
│   └── impl/        # 业务逻辑实现
└── util/            # 工具类
```

#### 前端架构
```
elm-vue/src/
├── assets/          # 静态资源
├── components/      # 组件
├── router/          # 路由配置
├── utils/           # 工具函数
├── views/           # 页面视图
├── App.vue          # 根组件
└── main.js          # 入口文件
```

### 3.2 关键代码实现

#### 后端核心代码

**文件上传配置**
```java
package org.example.elm.config;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Configuration;

import java.io.File;

@Configuration
public class UploadConfig {
    
    @Value("${upload.path:./uploads}")
    private String uploadPath;
    
    public String getUploadPath() {
        // 确保使用绝对路径
        File uploadDir = new File(uploadPath);
        if (!uploadDir.isAbsolute()) {
            // 如果是相对路径，转换为绝对路径
            uploadDir = new File(System.getProperty("user.dir"), uploadPath);
        }
        return uploadDir.getAbsolutePath();
    }
    
    public void setUploadPath(String uploadPath) {
        this.uploadPath = uploadPath;
    }
}
```

**用户登录接口**
```java
@PostMapping("/getUserByIdByPass")
public User getUserByIdByPass(@RequestParam String userId, @RequestParam String password) {
    return userService.getUserByIdByPass(userId, password);
}
```

**用户头像上传接口**
```java
@PostMapping("/uploadAvatar")
public Map<String, Object> uploadAvatar(@RequestParam("file") MultipartFile file, 
                                      @RequestParam("userId") String userId) {
    Map<String, Object> result = new HashMap<>();
    
    try {
        // 检查文件是否为空
        if (file.isEmpty()) {
            result.put("success", false);
            result.put("message", "文件不能为空");
            return result;
        }
        
        // 检查文件类型
        String contentType = file.getContentType();
        if (contentType != null && !contentType.startsWith("image/")) {
            result.put("success", false);
            result.put("message", "只允许上传图片文件");
            return result;
        }
        
        // 检查文件大小（限制为5MB）
        if (file.getSize() > 5 * 1024 * 1024) {
            result.put("success", false);
            result.put("message", "文件大小不能超过5MB");
            return result;
        }
        
        // 获取项目根目录
        String projectPath = System.getProperty("user.dir");
        // 创建上传目录
        String uploadDir = projectPath + "/uploads";
        File uploadDirectory = new File(uploadDir);
        if (!uploadDirectory.exists()) {
            uploadDirectory.mkdirs();
        }
        
        // 生成唯一的文件名
        String originalFilename = file.getOriginalFilename();
        String fileExtension = "";
        if (originalFilename != null && originalFilename.contains(".")) {
            fileExtension = originalFilename.substring(originalFilename.lastIndexOf("."));
        }
        String newFileName = userId + "_" + System.currentTimeMillis() + fileExtension;
        
        // 保存文件
        String filePath = uploadDir + "/" + newFileName;
        File destFile = new File(filePath);
        file.transferTo(destFile);
        
        // 更新数据库中的用户头像路径
        String avatarPath = "/uploads/" + newFileName;
        int updateResult = userService.updateUserAvatar(userId, avatarPath);
        
        if (updateResult > 0) {
            result.put("success", true);
            result.put("imageUrl", avatarPath);
            result.put("message", "头像上传成功");
        } else {
            result.put("success", false);
            result.put("message", "头像上传失败");
        }
    } catch (Exception e) {
        result.put("success", false);
        result.put("message", "头像上传异常: " + e.getMessage());
    }
    
    return result;
}
```

**食品列表接口**
```java
@PostMapping("/listFoodByBusinessId")
public List<Food> listFoodByBusinessId(@RequestParam Integer businessId) {
    return foodService.listFoodByBusinessId(businessId);
}
```

**订单创建接口**
```java
@PostMapping("/createOrders")
public int createOrders(@RequestParam String userId, @RequestParam Integer businessId, 
                       @RequestParam Integer daId, @RequestParam Double orderTotal) {
    return ordersService.createOrders(userId, businessId, daId, orderTotal);
}
```

#### 前端核心代码

**登录页面**
```vue
<template>
  <div class="login-container">
    <el-form :model="loginForm" :rules="rules" ref="loginForm">
      <el-form-item prop="userId">
        <el-input v-model="loginForm.userId" placeholder="请输入用户名"></el-input>
      </el-form-item>
      <el-form-item prop="password">
        <el-input v-model="loginForm.password" type="password" placeholder="请输入密码"></el-input>
      </el-form-item>
      <el-form-item>
        <el-button type="primary" @click="login" class="login-btn">登录</el-button>
        <el-button @click="register">注册</el-button>
      </el-form-item>
    </el-form>
  </div>
</template>

<script>
export default {
  data() {
    return {
      loginForm: {
        userId: '',
        password: ''
      },
      rules: {
        userId: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
        password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
      }
    }
  },
  methods: {
    login() {
      this.$refs.loginForm.validate((valid) => {
        if (valid) {
          this.$axios.post('/api/user/getUserByIdByPass', this.loginForm).then(res => {
            if (res.data) {
              localStorage.setItem('user', JSON.stringify(res.data));
              this.$message.success('登录成功');
              this.$router.push('/');
            } else {
              this.$message.error('用户名或密码错误');
            }
          });
        }
      });
    },
    register() {
      this.$router.push('/register');
    }
  }
}
</script>
```

**商家列表页面**
```vue
<template>
  <div class="business-list">
    <el-row :gutter="20">
      <el-col :span="6" v-for="business in businessList" :key="business.businessId">
        <el-card :body-style="{ padding: '0px' }">
          <img :src="business.businessImg" class="image" />
          <div style="padding: 14px;">
            <div class="business-name">{{ business.businessName }}</div>
            <div class="business-desc">{{ business.businessExplain }}</div>
            <div class="business-price">起送价: ¥{{ business.startPrice }} | 配送费: ¥{{ business.deliveryPrice }}</div>
            <el-button type="primary" @click="goToBusinessDetail(business.businessId)" style="width: 100%; margin-top: 10px;">
              进入商家
            </el-button>
          </div>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script>
export default {
  data() {
    return {
      businessList: []
    }
  },
  mounted() {
    this.loadBusinessList();
  },
  methods: {
    loadBusinessList() {
      this.$axios.get('/api/business/listBusiness').then(res => {
        this.businessList = res.data;
      });
    },
    goToBusinessDetail(businessId) {
      this.$router.push(`/business/${businessId}`);
    }
  }
}
</script>
```
```

### 3.3 功能界面截图

系统包含多个功能页面，以下是主要页面的功能描述：

#### 登录页面
- 用户登录入口，支持用户名和密码登录
- 包含表单验证，确保输入的有效性
- 提供注册入口，引导新用户注册

#### 注册页面
- 用户注册表单，收集用户基本信息
- 包含密码确认和表单验证
- 注册成功后自动跳转到登录页面

#### 首页/商家列表页面
- 展示所有商家信息，包括商家图片、名称、描述、起送价和配送费
- 支持按商家分类筛选
- 点击商家卡片可进入商家详情页面

#### 商家详情页面
- 展示商家详细信息
- 按分类展示商家的所有食品
- 提供食品搜索功能
- 点击食品可加入购物车

#### 购物车页面
- 展示当前购物车中的所有食品
- 支持修改食品数量和删除食品
- 自动计算商品总价
- 提供结算按钮，进入订单确认页面

#### 订单确认页面
- 展示订单信息，包括商家、食品列表、总价
- 选择配送地址
- 确认订单信息后提交订单

#### 订单列表页面
- 展示用户的所有历史订单
- 支持按订单状态筛选
- 点击订单可查看订单详情

#### 个人中心页面
- 展示用户个人信息
- 提供头像上传功能
- 支持修改个人信息
- 提供地址管理、订单管理等入口

#### 用户地址管理页面
- 展示用户的所有配送地址
- 支持添加、修改、删除地址
- 可设置默认地址

## 四、实训总结

### 4.1 主要收获
通过本次实训，我获得了以下收获：
- 掌握了Spring Boot + MyBatis + Vue.js的前后端分离开发模式
- 提高了Java企业级应用的开发能力
- 熟悉了Vue.js前端框架的组件化开发
- 学习了数据库设计和优化的方法
- 提升了独立解决问题和项目管理的能力
- 了解了在线订餐系统的完整业务流程和实现原理

### 4.2 作品特色
- 采用前后端分离架构，提高了系统的可维护性和扩展性
- 界面设计简洁美观，用户体验良好
- 实现了完整的在线订餐业务流程
- 采用RESTful API设计风格，接口规范统一
- 支持文件上传功能，方便商家和食品图片管理

### 4.3 碰到的问题及解决办法

**问题1：跨域请求失败**
- 原因：前端Vue项目和后端Spring Boot项目运行在不同的端口，浏览器的同源策略限制了跨域请求
- 解决办法：在后端配置CORS支持，允许前端域名的跨域请求

**问题2：文件上传功能实现**
- 原因：需要处理多部分表单数据和文件存储路径
- 解决办法：使用Spring Boot的MultipartFile接口处理文件上传，配置文件存储路径，实现文件上传和访问功能

**问题3：Vue组件之间的数据传递**
- 原因：组件化开发中需要在不同组件之间传递数据
- 解决办法：使用Vue的props、事件总线、Vuex状态管理等方式实现组件间通信

### 4.4 存在不足
- 系统的安全性考虑不够全面，如缺少权限控制和接口认证
- 没有实现实时消息通知功能
- 系统性能优化方面还有提升空间
- 移动端适配不够完善

### 4.5 改进建议
- 增加Spring Security权限控制，实现用户角色管理
- 集成WebSocket实现实时消息通知
- 优化数据库查询，提高系统性能
- 完善移动端适配，实现响应式设计
- 增加订单支付功能的实际支付接口对接
- 实现商家管理后台功能

---

**实训报告完成人**：
**完成日期**：2026年1月5日